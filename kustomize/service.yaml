---
apiVersion: v1
kind: Service
metadata:
  name: books
  namespace: beta-apps
  labels:
    env: development
spec:
  selector:
    app: books
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
---